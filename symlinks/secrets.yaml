- defaults:
    link:
      create: true
      force: true
- link:
    shell/init-scripts/secrets_autoload: modules/secrets/init-scripts/secrets_autload
# The idea is:
# Apply left part is applied in $DOTFILES_PATH and right part in the secrets repository